<%= form_for(@work_diary) do |f| %>
  <% if @work_diary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_diary.errors.count, "error") %> prohibited this work_diary from being saved:</h2>

      <ul>
      <% @work_diary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
