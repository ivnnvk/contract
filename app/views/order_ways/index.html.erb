<h2><%=t :Listing %>&nbsp;<%=t :order_ways_u %></h2>

<table cellspacing=15 >
  <tr>
    <td><%=t 'Name' %></td>
  </tr>
  <% @order_ways.each do |order_way| %>
    <tr>
      <td><%= order_way.name %></td>
      <td><%= link_to t('Show'), order_way %></td>
      <td><%= link_to t('Edit'), edit_order_way_path(order_way) %></td>
      <td><%= link_to t('Destroy'), order_way, method: :delete, data: { confirm: t('Are_you_sure?') } %></td>
    </tr>
  <% end %>
</table>

<br>

&nbsp;&nbsp;&nbsp;<%= link_to t('Home'), root_path %>
&nbsp;&nbsp;&nbsp;<%= link_to t('New'), new_order_way_path %>
