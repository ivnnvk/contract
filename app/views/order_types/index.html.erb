<h2><%=t :Listing %>&nbsp;<%=t :order_types_u %></h2>

<table cellspacing=15 >
  <tr>
    <td><%=t t('Name') %></td>
  </tr>
  <% @order_types.each do |order_type| %>
    <tr>
      <td><%= order_type.name %></td>
      <td><%= link_to t('Show'), order_type %></td>
      <td><%= link_to t('Edit'), edit_order_type_path(order_type) %></td>
      <td><%= link_to t('Destroy'), order_type, method: :delete, data: { confirm: t('Are_you_sure?') } %></td>
    </tr>
  <% end %>
</table>

<br>

&nbsp;&nbsp;&nbsp;<%= link_to t('Home'), root_path %>
&nbsp;&nbsp;&nbsp;<%= link_to t('New'), new_order_type_path %>
